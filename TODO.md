# TODO — Antigravity Bar
*Обновлено: 2026-03-25*

## 🔧 Задачи

- [x] **Оптимизация polling**: сейчас данные обновляются каждые 30 сек — добавить adaptive interval (чаще пока открыто меню, реже в фоне)
- [x] **Поддержка новых моделей**: добавить новые квоты/иконки в UI при появлении новых моделей Antigravity (сейчас: Flash, Pro, Claude)
- [x] **Новые macOS API**: проверить и мигрировать устаревшие AppKit API на актуальные (macOS 14/15)
- [x] **Daemon discovery**: защита от зависаний — добавить timeout + retry при чтении JSON из `~/.gemini/antigravity/daemon/`

## 💡 Tech Debt

- [x] Бинарник называется `StellarBar` — переименовать в `AntigravityBar` в `Package.swift` и `build-app.sh`


## Архитектурный Аудит и Рефакторинг 
### 🧹 Что почистить
- [x] Убрать временные JSON-файлы Daemon Discovery (если они остаются после закрытия Language Server).

### 🏗 Улучшение архитектуры
- [ ] Парсинг процессов macOS через `ps` и `lsof` — это хрупко (и ресурсоёмко). Лучше использовать нативные Darwin API (`sysctl`, `proc_pidinfo`) или XPC/NSXPCConnection. (Оставлено на будущую глобальную переработку C-Interop)
- [x] Изолировать логику работы с ФС (Brain/Code Tracker) за протоколы для легкого тестирования без доступа к реальному диску.

### ✂️ Минимизация и рефакторинг кода
- [x] Унифицировать методы парсинга квот: жесткие `String` ключи `["userStatus"]` перебить в красивые `Decodable` структуры.
